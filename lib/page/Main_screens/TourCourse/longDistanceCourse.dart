import 'package:flutter/material.dart';

class LongDistanceCourse extends StatelessWidget {
  const LongDistanceCourse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
