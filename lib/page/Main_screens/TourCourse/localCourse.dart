import 'package:flutter/material.dart';

class LocalCourse extends StatelessWidget {
  const LocalCourse({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
