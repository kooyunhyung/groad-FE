import 'package:flutter/material.dart';

class ShortDistanceCourse extends StatelessWidget {
  const ShortDistanceCourse({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
